---
layout: post
title: Year 2025 Review!
tags:
  - Work
  - Life
  - Review
permalink: /blog/year-2025-review
travelYear: "2025"
---

<div style="text-align: center;">
<img src="/assets/2025-review/hero.jpg" alt="Hero Image" width="700" style="max-height: 600px; object-fit: cover; object-position: center; display: block; margin: 0 auto;">
<p style="font-style: italic; margin-top: 5px;">Idli from The Rameshwaram Cafe, Bengaluru</p>
</div>

<!-- Introduction paragraph about the year -->

OK, 2026 is here now! Keeping the tradition alive, here is another yearly review blog. Ever since I started writing these,
it has never been this difficult to write, thanks to AI, I can barely write anything anymore, but I'll try to whatever I 
can for 2025.

## Work

<!-- Overview of work in 2025 -->

<div style="max-width: 800px; margin: 2em auto;">
  <canvas id="githubCommitsChart"></canvas>
</div>

<script src="https://cdn.jsdelivr.net/npm/chart.js@4.4.0/dist/chart.umd.min.js"></script>
<script>
(function() {
  // Detect dark mode via data-theme attribute
  const isDarkMode = document.documentElement.getAttribute('data-theme') === 'dark';
  const textColor = isDarkMode ? '#e0e0e0' : '#333333';
  const gridColor = isDarkMode ? 'rgba(255, 255, 255, 0.1)' : 'rgba(0, 0, 0, 0.1)';

  Chart.defaults.font.family = '"Crimson Pro", "Helvetica Neue", Helvetica, Arial, sans-serif';
  Chart.defaults.font.size = 16;
  Chart.defaults.color = textColor;

  const ctx = document.getElementById('githubCommitsChart');
  const chart = new Chart(ctx, {
    type: 'line',
    data: {
      labels: ['Jan', 'Feb', 'Mar', 'Apr', 'May', 'Jun', 'Jul', 'Aug', 'Sep', 'Oct', 'Nov', 'Dec'],
      datasets: [{
        label: 'Commits',
        data: [228, 415, 412, 145, 313, 485, 493, 399, 394, 546, 440, 193],
        backgroundColor: 'rgba(54, 162, 235, 0.1)',
        borderColor: 'rgba(54, 162, 235, 1)',
        borderWidth: 2,
        fill: true,
        tension: 0.4,
        pointRadius: 4,
        pointBackgroundColor: 'rgba(54, 162, 235, 1)',
        pointBorderColor: isDarkMode ? '#2d2d2d' : '#fff',
        pointBorderWidth: 2,
        pointHoverRadius: 6
      }]
    },
    options: {
      responsive: true,
      maintainAspectRatio: true,
      plugins: {
        legend: {
          display: false
        },
        title: {
          display: true,
          text: 'My GitHub Commits in 2025 (Total: 4,463)',
          color: textColor,
          font: {
            size: 18,
            weight: 'normal',
            family: '"Crimson Pro", "Helvetica Neue", Helvetica, Arial, sans-serif'
          }
        }
      },
      scales: {
        y: {
          beginAtZero: true,
          grid: {
            color: gridColor
          },
          ticks: {
            stepSize: 100,
            color: textColor,
            font: {
              family: '"Crimson Pro", "Helvetica Neue", Helvetica, Arial, sans-serif',
              size: 16
            }
          },
          title: {
            display: true,
            text: 'Number of Commits',
            color: textColor,
            font: {
              family: '"Crimson Pro", "Helvetica Neue", Helvetica, Arial, sans-serif',
              size: 16
            }
          }
        },
        x: {
          grid: {
            color: gridColor
          },
          ticks: {
            color: textColor,
            font: {
              family: '"Crimson Pro", "Helvetica Neue", Helvetica, Arial, sans-serif',
              size: 16
            }
          },
          title: {
            display: true,
            text: 'Month',
            color: textColor,
            font: {
              family: '"Crimson Pro", "Helvetica Neue", Helvetica, Arial, sans-serif',
              size: 16
            }
          }
        }
      }
    }
  });

  // Update chart when theme changes
  const observer = new MutationObserver((mutations) => {
    mutations.forEach((mutation) => {
      if (mutation.attributeName === 'data-theme') {
        const isDark = document.documentElement.getAttribute('data-theme') === 'dark';
        const newTextColor = isDark ? '#e0e0e0' : '#333333';
        const newGridColor = isDark ? 'rgba(255, 255, 255, 0.1)' : 'rgba(0, 0, 0, 0.1)';

        chart.options.plugins.title.color = newTextColor;
        chart.options.scales.y.ticks.color = newTextColor;
        chart.options.scales.y.title.color = newTextColor;
        chart.options.scales.y.grid.color = newGridColor;
        chart.options.scales.x.ticks.color = newTextColor;
        chart.options.scales.x.title.color = newTextColor;
        chart.options.scales.x.grid.color = newGridColor;
        chart.data.datasets[0].pointBorderColor = isDark ? '#2d2d2d' : '#fff';
        chart.update();
      }
    });
  });

  observer.observe(document.documentElement, {
    attributes: true,
    attributeFilter: ['data-theme']
  });
})();
</script>

<div style="max-width: 800px; margin: 2em auto;">
  <canvas id="weekdayCommitsChart"></canvas>
</div>

<script>
(function() {
  // Detect dark mode via data-theme attribute
  const isDarkMode = document.documentElement.getAttribute('data-theme') === 'dark';
  const textColor = isDarkMode ? '#e0e0e0' : '#333333';
  const gridColor = isDarkMode ? 'rgba(255, 255, 255, 0.1)' : 'rgba(0, 0, 0, 0.1)';

  Chart.defaults.font.family = '"Crimson Pro", "Helvetica Neue", Helvetica, Arial, sans-serif';
  Chart.defaults.font.size = 16;
  Chart.defaults.color = textColor;

  const ctx = document.getElementById('weekdayCommitsChart');
  const chart = new Chart(ctx, {
    type: 'bar',
    data: {
      labels: ['Sunday', 'Monday', 'Tuesday', 'Wednesday', 'Thursday', 'Friday', 'Saturday'],
      datasets: [{
        label: 'Average Commits',
        data: [10.0, 16.19, 14.98, 10.6, 16.33, 11.67, 5.85],
        backgroundColor: 'rgba(75, 192, 192, 0.6)',
        borderColor: 'rgba(75, 192, 192, 1)',
        borderWidth: 2
      }]
    },
    options: {
      responsive: true,
      maintainAspectRatio: true,
      plugins: {
        legend: {
          display: false
        },
        title: {
          display: true,
          text: 'Average GitHub Commits by Day of Week (2025)',
          color: textColor,
          font: {
            size: 18,
            weight: 'normal',
            family: '"Crimson Pro", "Helvetica Neue", Helvetica, Arial, sans-serif'
          }
        }
      },
      scales: {
        y: {
          beginAtZero: true,
          grid: {
            color: gridColor
          },
          ticks: {
            stepSize: 2,
            color: textColor,
            font: {
              family: '"Crimson Pro", "Helvetica Neue", Helvetica, Arial, sans-serif',
              size: 16
            }
          },
          title: {
            display: true,
            text: 'Average Commits per Day',
            color: textColor,
            font: {
              family: '"Crimson Pro", "Helvetica Neue", Helvetica, Arial, sans-serif',
              size: 16
            }
          }
        },
        x: {
          grid: {
            color: gridColor
          },
          ticks: {
            color: textColor,
            font: {
              family: '"Crimson Pro", "Helvetica Neue", Helvetica, Arial, sans-serif',
              size: 16
            }
          },
          title: {
            display: true,
            text: 'Day of Week',
            color: textColor,
            font: {
              family: '"Crimson Pro", "Helvetica Neue", Helvetica, Arial, sans-serif',
              size: 16
            }
          }
        }
      }
    }
  });

  // Update chart when theme changes
  const observer = new MutationObserver((mutations) => {
    mutations.forEach((mutation) => {
      if (mutation.attributeName === 'data-theme') {
        const isDark = document.documentElement.getAttribute('data-theme') === 'dark';
        const newTextColor = isDark ? '#e0e0e0' : '#333333';
        const newGridColor = isDark ? 'rgba(255, 255, 255, 0.1)' : 'rgba(0, 0, 0, 0.1)';

        chart.options.plugins.title.color = newTextColor;
        chart.options.scales.y.ticks.color = newTextColor;
        chart.options.scales.y.title.color = newTextColor;
        chart.options.scales.y.grid.color = newGridColor;
        chart.options.scales.x.ticks.color = newTextColor;
        chart.options.scales.x.title.color = newTextColor;
        chart.options.scales.x.grid.color = newGridColor;
        chart.update();
      }
    });
  });

  observer.observe(document.documentElement, {
    attributes: true,
    attributeFilter: ['data-theme']
  });
})();
</script>

### AQI Monitor

<!-- Description of AQI Monitor project -->

<div style="text-align: center;">
<img src="/assets/2025-review/aqi-monitor.jpg" alt="AQI Monitor" width="500" style="display: block; margin: 0 auto;">
<p style="font-style: italic; margin-top: 5px;">AQI Monitor</p>
</div>

### Project 2

<!-- Description of project 2 -->

### Cirun

<!-- Updates on Cirun -->

### Homelab

<!-- Any homelab updates -->

## Personal Life

### Theatre / TV

<!-- Any theatre or TV appearances -->

### Misc

- First 10K run in Victoria Park
- Item 2
- Item 3

<div style="text-align: center;">
<img src="/assets/2025-review/10k_medal.jpg" alt="10K Medal" width="400" style="display: block; margin: 0 auto;">
<p style="font-style: italic; margin-top: 5px;">First 10K medal</p>
</div>

### Travel

I travelled to a few places this year:

- **United Kingdom**: London, Edinburgh, Cambridge, Oxford, Newcastle upon Tyne, York, Northampton
- **India**: Delhi, Gurgaon, Bengaluru, Kozhikode
- **France**: Paris, Versailles
- **Netherlands**: Amsterdam
- **Finland**: Helsinki

### Events

Here are some of the interesting events I went to this year:

- India vs England Test Match at Lord's
- Legends League Cricket: India vs South Africa
- Edinburgh Fringe Festival
- Amjad Ali Khan concert at Royal Festival Hall
